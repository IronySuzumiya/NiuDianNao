#ifndef __COMMON__
#define __COMMON__

#include <iostream>
#include <cstddef>
#include <queue>
#include <string>
#include <assert.h>

typedef int64_t mem_addr;
typedef int64_t mem_size;

#endif
